<div class="page-header">
	<h2><%= "#{t("logo")} #{t("new")}" %> </h2>
</div>
<%= render 'form' %>
<%= link_to t('Back'), business_client_path(:id => @business_client, :page => @current_page), :class => "btn btn-link" %>
