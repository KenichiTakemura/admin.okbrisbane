<%= render :partial => 'posts_managements/shared/head' %>
<h2 class="title">	<%= t("#{@category}") %> #<%= @post.id %> <%= t('Edit')%></h2>
<div class="inner">
	<%= render 'posts_managements/shared/form' %>
<%= link_to t('Show'), show_post_path(@category, @post, @current_page) %> |
<%= link_to t('Back'), posts_managements_path(:category => @category, :page => @current_page) %>
</div>
