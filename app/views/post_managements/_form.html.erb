<%= form_for(@post_management) do |f| %>
  <% if @post_management.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_management.errors.count, "error") %> prohibited this post_management from being saved:</h2>

      <ul>
      <% @post_management.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
