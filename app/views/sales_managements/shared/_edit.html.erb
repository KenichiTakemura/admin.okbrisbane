<h2 class="title">	<%= t("#{@category}") %> #<%= @post.id %> <%= t('Edit')%></h2>
<div class="inner">
	<%= render 'sales_managements/shared/form' %>
<%= link_to t('Show'), show_management_path(@post, @category, @current_page) %> |
<%= link_to t('Back'), management_path(@category, @current_page) %>
</div>
