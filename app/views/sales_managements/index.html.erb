<%= render :partial => 'sales_managements/shared/head' %>
<div class="content">
	<% if !@category %>
	<h2 class="title"><%= t('sales_management.title')%></h2>
	<div class="inner">
		<p>
			<%= t("sales_management.description")%>
		</p>
	</div>
	<% else %>

	<h2 class="title"><%= t("listing_#{@category}")%></h2>
	<div class="inner">
		<%= link_to t("new_#{@category}"), new_category_path(@category) %>
		<div style="float:right"><%= paginate @collection %></div>
		<table class="table">
			<tr>
				<th class="first"><%= t("id") %></th>
				<th><%= t("subject") %></th>
				<th><%= t("autor") %></th>
				<th><%= t("created_at") %></th>
				<th><%= t("image") %></th>

				<th class="last"></th>
			</tr>
			<% if @collection.nil? || @collection.empty? %>
			<tr><td colspan="6">
			<p>
				<%= t("no_information")%>
			</p>
			</td></tr>
			<% else %>
			<% @collection.each do |item| %>
			<tr class="<%= cycle("odd", "even") %>" >
				<td><%= item.id%></td>
				<td><%= _truncate(item.subject) %></td>
				<td><%= item.posted_by.email if !item.posted_by.nil? %></td>
				<td><%= Common.date_format(item.created_at) %></td>
				<td>
					<% item.image.each do |image| %>
					<%= thumbnail(image, estate_delete_image_path(item, :image => image.id)) %>
					<% end %>
				</td>
				<td><%= link_to t('Show'), show_category_parh(@category, item) %><br/><%= link_to t('Edit'), edit_category_path(@category, item) %><br/>
					<%= link_to t("delete_#{@category}"), item, method: :delete, data: { confirm: t('confirm_delete') } %></td>
			</tr>
			<% end %>
					<% end %>
		</table>


	</div>
		<% end %>
</div>
